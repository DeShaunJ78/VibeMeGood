Add 4 fail safes. Do all 4 in order.
Test each before moving to the next.

FAIL SAFE 1 — Projection Confidence Indicator
Data already exists in our_projections:
gamesUsed and dataQualityScore columns.

In the market-intel API response add these
fields per prop:
gamesUsed: number (from our_projections.games_used)
dataQualityScore: number (from our_projections.data_quality_score)

In slate-board.tsx on each prop row show:
- gamesUsed < 5: amber badge "LOW CONFIDENCE"
- gamesUsed 5-20: small grey text "MED"
- gamesUsed >= 20: nothing shown

Place badge next to the projection gap value.

FAIL SAFE 2 — Stale Data Hard Stop
When last external-odds sync is over 4 hours old:
Hide the True Edge column entirely on Slate Board.
Show a banner at top of Slate Board:
"⚠️ Odds data stale — sync before acting on edge scores"
with a Sync Odds button that triggers the sync.

Check staleness from:
SELECT finished_at FROM sync_runs
WHERE job_name = 'external-odds'
ORDER BY finished_at DESC LIMIT 1

If finished_at < now - 4 hours: trigger the banner.

FAIL SAFE 3 — Calibration Warning
On Today's Edges and prop detail sheet:
When edge score is shown but calibration_records
count is below 30 for this sport + stat type:
Show small amber badge: "LOW SAMPLE"
Tooltip: "Edge score based on limited calibration
data. Treat with caution until 30+ results logged."

Query:
SELECT COUNT(*) FROM calibration_records
WHERE sport = ? AND stat_type = ?

Add this check to the market-intel response
as a calibrationCount field per prop.

FAIL SAFE 4 — 99th Percentile Ceiling
Add p99 column to our_projections table:
ALTER TABLE our_projections
ADD COLUMN IF NOT EXISTS p99 numeric;

In lib/projection/compute.ts after computing
mean and effectiveStd:
const p99 = mean + 2.33 * effectiveStd;

Save to our_projections p99 column.

Show in Entry Builder simulation results:
"Monster ceiling (99th): X.X pts"

After all 4 fail safes:
Run pnpm run typecheck — must pass
Run pnpm run build — must pass
Publish to production

Report PASS or FAIL for each fail safe.
