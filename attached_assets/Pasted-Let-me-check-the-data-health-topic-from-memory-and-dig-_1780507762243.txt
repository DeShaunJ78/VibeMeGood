Let me check the data health topic from memory and dig into both the API health endpoint and UI code to find the root cause:


6 actions
Let me look at the full check logic in the settings page and the projection compute routes:


6 actions
I can see exactly what's happening. The freshness checks look for data_pull_logs rows with specific jobName values. Let me check what the cron schedule actually does and whether startup compute writes those logs:


3 actions
Found it. Two bugs:

Variance jobName mismatch — cron logs as "variance-scores" but system-health.ts looks for "variance"
Projections — startup runs computeAllProjections() directly (no log written), so at 1:02 AM that was likely the 1am window but the cron only runs at 6/11/14. If the production server restarts after 11am, the next scheduled run is 2pm
Let me verify both and check the startup path:


3 actions
Two bugs confirmed:

Variance cron logs "variance-scores" but system-health.ts reads for "variance" — cron runs are invisible to the health check
Startup projection run calls computeAllProjections() bare with no log entry — so a fresh deploy (like just happened) leaves projections stale until the next scheduled cron window
Let me see logPull and the startup code to plan the fixes:


5 actions
Two clear bugs:

index.ts startup calls computeAllProjections() and computeAllVarianceScores() bare — no log entry written, so health check sees the DB's last real cron timestamp
Variance cron logs "variance-scores" but system-health.ts reads "variance" — every scheduled run is invisible to the health check
Both fixes are in cron.ts and index.ts:


3 actions
Now fix index.ts to route the startup run through logPull so every deploy immediately stamps a fresh log:


3 actions
Now let me typecheck and restart to verify:


2 actions
Clean typecheck. Restarting the API server:


4 actions
The logs confirm both fixes landed perfectly:

Sync completed  provider: "nba-stats"  jobName: "projections"  recordsProcessed: 39
Sync completed  provider: "internal"   jobName: "variance"     recordsProcessed: 39
Startup projection run complete  computed: 39

Both rows are now in data_pull_logs with fresh timestamps. Here's what was wrong and what changed:

Bug 1 — Variance cron used the wrong job name
The scheduled runs (6:30 AM / 6:30 PM) were writing "variance-scores" to the log table, but the health check was querying for "variance". Every cron run was invisible. Fixed by aligning the cron to "variance".

Bug 2 — Startup run had no log entry
index.ts was calling computeAllProjections() and computeAllVarianceScores() directly without writing any data_pull_logs row. So after every deploy/restart, the health check fell back to whatever old timestamp was in the DB — making Projections and Variance look perpetually stale even though the data was freshly computed. Fixed by routing the startup through logPull, the same wrapper the cron uses.

Going forward, every server restart — including production deploys — will immediately stamp fresh timestamps and both checks will go green within about 3 seconds of boot.


Checkpoint made
15 minutes ago

Worked for 3 minutes
Time worked
3 minutes
Work done
22 actions
Items read
822 lines
Code changed
+43
-31
Agent Usage
$0.81
is it fixed? you did this yesterday for me only to come here and encounter this shit! did you do it right? do you test it idiot?